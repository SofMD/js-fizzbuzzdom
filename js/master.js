// generazione ciclo da 1 a 100
let boxes = document.querySelector('.boxes');

for (let box = 1; box <= 100; box++) {
    console.log(box);
    boxes.innerHTML += ` <div class="square box-${box}">${box}</div>`;

    // calcolo numeri divisibili per 3 e 5

    if(box % 3 === 0){
     
    }
    else if(box % 3 === 0){

    }
    else if (box % 3 === 0) && (box % 3 === 0){
        
    }
}